spring:
  profiles:
    active: dev

  application:
    name: nayepankh-vims

server:
  port: ${PORT:8080}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
  api-docs:
    path: /v3/api-docs
